<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.knightzz.mapper.AclUserMapper">

    <resultMap id="BaseResultMap" type="cn.knightzz.entity.AclUser">
        <id property="id" column="id" jdbcType="CHAR"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="nickName" column="nick_name" jdbcType="VARCHAR"/>
        <result property="isDeleted" column="is_deleted" jdbcType="BOOLEAN"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,
        nick_name,is_deleted,create_time,
        update_time
    </sql>
    <update id="updatePassword">
        update acl_user
        set password = #{password}
        where id = #{id}
    </update>

    <!-- 根据用户名查询用户信息 -->
    <select id="selectByUsername" resultType="cn.knightzz.entity.AclUser">
        select *
        from acl_user
        where username = #{username}
    </select>
</mapper>
